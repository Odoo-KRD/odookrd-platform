/**
 * Mirrors MAX_CATEGORY_DEPTH in the API's knowledge.rules.ts. The API is the
 * enforcer; this only keeps the portal from offering choices it would reject.
 */
export const MAX_CATEGORY_DEPTH = 3;
